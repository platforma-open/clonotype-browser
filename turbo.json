{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["tsconfig.json"],
  "tasks": {
    "type-check": {
      "outputs": [],
      "dependsOn": ["^build"]
    },
    "linter": {
      "outputs": [],
      "dependsOn": ["^build"]
    },
    "formatter": {
      "outputs": [],
      "dependsOn": ["^build"]
    },
    "build": {
      "inputs": ["$TURBO_DEFAULT$"],
      "env": ["PL_PKG_DEV", "PL_DOCKER_REGISTRY_PUSH_TO"],
      "outputs": ["./dist/**", "./block-pack/**", "./pkg-*.tgz"],
      "dependsOn": ["type-check", "linter", "formatter", "^build"]
    },
    "do-pack": {
      "dependsOn": ["build"],
      "outputs": ["package.tgz"]
    },
    "test": {
      "dependsOn": ["build"],
      "passThroughEnv": ["PL_ADDRESS", "PL_TEST_PASSWORD", "PL_TEST_USER", "PL_TEST_PROXY", "DEBUG", "PL_DOCKER_REGISTRY_PUSH_TO"]
    },
    "mark-stable": {
      "passThroughEnv": ["PL_REGISTRY", "AWS_*"],
      "cache": false
    }
  }
}
